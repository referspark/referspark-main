CREATE TABLE business (id   INT, 
                      user_name VARCHAR(80),
                      email     VARCHAR(80),
                      password  VARCHAR(256),
                      name      VARCHAR(80));
                              
CREATE TABLE customer (id INT,
                       name       VARCHAR(80),
                       email      VARCHAR(80),
                       mobile     VARCHAR(20));                            